package com.mysportsshare.mysportscast.utils;

public interface CommentCallBackListener {
	/*method: updateCommentsCnt
	 * Desc: Used to update comments count on 
	 * Inputs:
	 * 		message: data which is being passed
	 * 		toFragment: the destination fragment
	 * 		
	 * */
	
	public void updateCommentsCnt(int mediaPosition, int commentCount);
}
